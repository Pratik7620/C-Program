#include<stdio.h>
#include<conio.h>
int main()
{
    char sub[20]="\0";
    int len=0;
    printf("\n Enter The String To Calculate its Length :");
    //scanf("%s",&Name);
    //gets(Name);
    scanf("%[^\n]",&sub);
    while(sublen]!='\0')
    {
        len++;
    }
    printf("\n Length Of %s Is=%d",sub,len);
    getch();
    return 0;
}
